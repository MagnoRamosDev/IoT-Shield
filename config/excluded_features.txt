# Identificadores estáticos (Gera Overfitting em portas e protocolos específicos)
src_port
dst_port
protocol

# Métricas absolutas de contagem de bytes (Focam no tempo total do fluxo e não no comportamento)
src2dst_bytes
dst2src_bytes

# Tamanhos Mínimos e Máximos de Pacotes 
# (Excluídos para forçar a IA a não "decorar" o tamanho exato do payload do malware)
bidirectional_min_ps
src2dst_min_ps
dst2src_min_ps
bidirectional_max_ps
src2dst_max_ps
dst2src_max_ps
src2dst_mean_ps
bidirectional_mean_ps